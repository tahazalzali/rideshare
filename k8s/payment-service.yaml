apiVersion: apps/v1
kind: Deployment
metadata: { name: payment-service, namespace: rides }
spec:
  replicas: 1
  selector: { matchLabels: { app: payment-service } }
  template:
    metadata: { labels: { app: payment-service } }
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/your-org/payment-service:latest
          ports: []
          env:
            - name: RABBITMQ_URI
              value: amqp://rabbitmq:rabbitmq@rabbitmq.rides.svc.cluster.local:5672
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secrets
                  key: jwt-secret
---
apiVersion: v1
kind: Service
metadata: { name: payment-service, namespace: rides }
spec:
  selector: { app: payment-service }
  ports:
    - name: http
      port: 80
      targetPort: 3000
