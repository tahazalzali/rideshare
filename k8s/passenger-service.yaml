apiVersion: apps/v1
kind: Deployment
metadata: { name: passenger-service, namespace: rides }
spec:
  replicas: 1
  selector: { matchLabels: { app: passenger-service } }
  template:
    metadata: { labels: { app: passenger-service } }
    spec:
      containers:
        - name: passenger-service
          image: ghcr.io/your-org/passenger-service:latest
          ports: []
          env:
            - name: RABBITMQ_URI
              value: amqp://rabbitmq:rabbitmq@rabbitmq.rides.svc.cluster.local:5672
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: gateway-secrets
                  key: jwt-secret
---
apiVersion: v1
kind: Service
metadata: { name: passenger-service, namespace: rides }
spec:
  selector: { app: passenger-service }
  ports:
    - name: http
      port: 80
      targetPort: 3000
